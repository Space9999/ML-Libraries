# ML-Libraries
A collection of machine learning algorithms, neural network components, and optimization techniques implemented from scratch using only NumPy. This repository recreates the core functionality of popular machine learning libraries to provide a transparent view of how models are built, trained, and evaluated under the hood.

If you want to use this repository, just git clone the repository.
